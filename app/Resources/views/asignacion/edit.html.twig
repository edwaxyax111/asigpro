{% extends 'base.html.twig' %}

{% block body %}
    <h1>Asignacion Editar</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-primary" type="submit" value="Editar" />
    {{ form_end(edit_form) }}

    <ul>
  
            <a class="btn btn-secondary" href="{{ path('asignacion_index') }}">Regresar</a>
           
    </ul>
{% endblock %}
