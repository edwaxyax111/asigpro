{% extends 'base.html.twig' %}

{% block body %}
    <h1>Asignacion creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn btn-secondary" href="{{ path('asignacion_index') }}">Regresar al listado</a>
        </li>
    </ul>
{% endblock %}
